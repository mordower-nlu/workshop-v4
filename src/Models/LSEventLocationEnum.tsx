/**
 * 
 * Our events typically take place at certain locations.
 * The most common locations are available via this enum.
 * 
 * 
 */

export enum LSEventLocation {
    STUDYTABLES,
    LSSPACE,
    DEN,
    ATRIUM,
    WHEELING,
    TBD,
    Other
}